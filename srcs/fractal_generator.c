void	generate_fractal(char *fract_type, t_fract *fract)
{
	//strcmp magic
	if ()
		gen_mandel(fract);
	else if ()
		gen_julia(fract);
	else if ()
		gen_ship(fract);
}

void	gen_mandel(t_fract *fract)
{
	// long ass maths,  
}

void	gen_julia(t_fract *fract)
{
	//maths
}

void	gen_ship(t_fract *fract)
{
	//maths
}
